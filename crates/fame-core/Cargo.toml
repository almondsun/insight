[package]
name = "fame-core"
version = "0.2.0-preview.1"
edition = "2021"
license = "MIT"
description = "Network-independent Fame scoring and protocol foundations for insIGht"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
